# anikate-landing (generic placeholder)

A simple, fast landing page + tiny blog section.

## Deploy (GitHub Pages)

- Push to GitHub
- Enable Pages: Settings → Pages → Deploy from branch → `main` / root

The site is plain HTML/CSS (no build step).
